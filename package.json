{
  "name": "pluto-talent-atlas",
  "version": "1.0.0",
  "private": true,
  "description": "Pluto's liquid-glass talent marketplace experience",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "build": "node --check server.mjs && node --check render-api.mjs && node --check marketplace.mjs && node --check login.mjs",
    "test": "node --test tests/accounts.test.mjs tests/marketplace.test.mjs",
    "db:generate": "drizzle-kit generate"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "^0.2.0",
    "drizzle-kit": "^0.31.10",
    "esbuild": "^0.28.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "jose": "^6.2.12",
    "pg": "^8.23.0"
  }
}
