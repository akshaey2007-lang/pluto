import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Create a Talent Account — Pluto', description: 'Create a Pluto talent profile for curated freelance opportunities and protected projects.' };
export default function TalentSignupLayout({ children }: { children: React.ReactNode }) { return children; }
