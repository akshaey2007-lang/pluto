import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Create a Client Account — Pluto', description: 'Create a Pluto client account for curated talent, funded milestones, and clear project management.' };
export default function ClientSignupLayout({ children }: { children: React.ReactNode }) { return children; }
