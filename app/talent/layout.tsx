import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Talent Workspace — Pluto', description: 'Pluto’s dedicated workspace for independent talent.' };
export default function TalentLayout({ children }: { children: React.ReactNode }) { return children; }
